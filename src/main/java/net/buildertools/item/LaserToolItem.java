package net.buildertools.item;

import net.minecraft.world.item.Item;

/**
 * Projects a laser beam from the player's eye to the block being looked at (client-side only).
 */
public class LaserToolItem extends Item {
    public LaserToolItem(Properties properties) {
        super(properties);
    }
}
